{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "nodemon app.js",
        "start-server": "node app.js"
    },
    "dependencies": {
        "dotenv": "^16.0.0",
        "mongodb": "^4.3.1"
    },
    "devDependencies": {
        "nodemon": "^2.0.15"
    }
}
